# render.yaml for Render Free deployment
services:
  - type: web
    name: Rava-server
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    branch: main
    autoDeploy: true
